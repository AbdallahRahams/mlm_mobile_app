import 'package:flutter/material.dart';

class ReferralsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Referrals Page')),
      body: Center(
        child: Text('Referrals Page Content'),
      ),
    );
  }
}
