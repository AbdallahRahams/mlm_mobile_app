import 'package:flutter/material.dart';

class ReferralDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Referral Details')),
      body: Center(
        child: Text('Referral Details Content'),
      ),
    );
  }
}
