import 'package:flutter/material.dart';

class EarningsDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Earnings Details')),
      body: Center(
        child: Text('Earnings Details Content'),
      ),
    );
  }
}