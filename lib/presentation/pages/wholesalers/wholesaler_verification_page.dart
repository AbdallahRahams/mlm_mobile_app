import 'package:flutter/material.dart';

class WholesalerVerificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Wholesaler Verification")),
      body: Center(child: Text("Wholesaler Verification Page Content")),
    );
  }
}
