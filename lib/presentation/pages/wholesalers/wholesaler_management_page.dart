import 'package:flutter/material.dart';

class WholesalerManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Wholesaler Management")),
      body: Center(child: Text("Wholesaler Management Page Content")),
    );
  }
}
