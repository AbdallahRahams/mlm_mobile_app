import 'package:flutter/material.dart';

class NetworkDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Network Details')),
      body: Center(
        child: Text('Network Details Content'),
      ),
    );
  }
}
